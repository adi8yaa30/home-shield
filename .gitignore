# macOS
.DS_Store
._*

# Local tool settings (launch.json is shared, this one is per-machine)
.claude/settings.local.json

# Design sources and raw masters (Figma exports, the original video files with
# audio) ARE tracked, deliberately — they are the only copies, so the repo is
# also the backup. They are not used by the site: the shipped assets are the
# .webp images and the encoded cuts in assets/video/.

# Editor / OS noise
Thumbs.db
*.swp
